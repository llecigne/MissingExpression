2007-08-03  Laurent Lecigne  <llecigne@free.fr>

	* src/missing-css-selector.js
	(ADD): convenience functions for previous/next siblings access.
	(UPDATE): attribute selector parsing.
	(UPDATE): shorter combinators implementation.

2007-01-14  Laurent Lecigne  <llecigne@free.fr>

	* src/missing-css-selector.js
 	(UPDATE): enhanced attribute selectors matching.
	(ADD): last-child pseudo class support.
	(ADD): general sibling selector partial support.

2007-01-08  Laurent Lecigne  <llecigne@free.fr>

	* src/missing-css-selector.js
	(ADD): apply() method to CSS selector.
	(UPDATE): shortened source code where possible.

2006-12-23  Laurent Lecigne  <llecigne@free.fr>

	* src/missing-examples.js
	(UPDATE): no longer calls Object.overload but Object.extend
	instead.

	* src/missing-inheritance.js
	(UPDATE): revert back to Object.extend, should have no side effect
	with existing Prototype implementation.
	(FIX): Object.extend returns object so as to be kind of backward
	compatible with Prorotype implementation.

	* src/missing-behaviour.js
	(UPDATE): implementation now uses document.visit.

	* src/missing-css-selector.js
	(ADD): select() support.
	(UPDATE): implementation, all selector now inherits from
	CssSelector.Abstract.
	(FIX): AttributeSelector match.

2006-12-20  Laurent Lecigne  <llecigne@free.fr>

	* src/missing-css-selector.js
	(FIX): complex selector hierarchy.

2006-12-19  Laurent Lecigne  <llecigne@free.fr>

	* src/missing-css-selector.js
	(FIX): wrong behaviour when whitespaces around complex
	operator (space, + or >).

2006-12-18  Laurent Lecigne  <llecigne@free.fr>

	* src/missing-css-selector.js
	(ADD): first-child pseudo class support.

2006-12-14  Laurent Lecigne  <llecigne@free.fr>

	* src/missing-css-selector.js
	(ADD): selector priority setup.

	* src/missing-behaviour.js
	(UPDATE): optimized implementation.
	(FIX): sort rules according to their selector priority.

2006-11-29  Laurent Lecigne  <llecigne@free.fr>

	* src/missing-core.js
	(NEW): provides feature dependency support.

	* src/missing-behaviour.js
	(NEW): first draft.
	(UPDATE): add feature support.

	* src/missing-css-selector.js
	(REMOVE): some obsolete code.
	(FIX): universal selector build.
	(UPDATE): add feature support.

2006-11-28  Laurent Lecigne  <llecigne@free.fr>

	* src/missing-css-selector.js
	(UPDATE): remove factory methods from CSS selector classes,
	introduced a SimpleSelector class as described in CSS 2.0
	specification (chpt. 5.2).

2006-11-26  Laurent Lecigne  <llecigne@free.fr>

	* src/missing-css-selector.js
	(UPDATE): optimize regular expressions used for building type, ID,
	class and attribute selectors.
	(UPDATE): delegate type handling to TypeSelector within ID, class
	and attribute selectors.

2006-11-25  Laurent Lecigne  <llecigne@free.fr>

	* src/missing-css-selector.js: 
	(NEW): first draft.

2006-11-24  Laurent Lecigne  <llecigne@free.fr>

	* src/missing-examples.js 
	(NEW): moved from missing-aspect-examples.
	(ADD): Ajax.BusyRequest example.

	* src/missing-inheritance.js
	(ADD): a Function.prototype.extends method, convenient for
	defining a class that extend another.
	(UPDATE): Object.inherit renamed to extend. Note that now the
	'ancestor' is affected by this method, and leaves the extension
	untouched.
	
	* src/missing-aspect.js
	(ADD): original() method to be accessible on before/after aspect
	wrappers.
	(UPDATE): applyAround() method implementation.
	(UPDATE): as super is a potential future javascript keyword, it
	has been renamed callNext.

